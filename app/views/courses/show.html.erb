<h1>
  <%= @course.name %>
</h1>
<br/>
<%= link_to "Add a chapter to this course", new_chapter_path(course_id: @course.id), class: "btn btn-lg btn-primary" %>
<br/>
<%= link_to "delete this course", course_path(course_id: @course.id), method: :delete%>
<br/>
<%= link_to "edit this course name", edit_course_path%>
<br/>
<h2> List of Chapters </h2>
<% if @chapters.any? %>
  <ul class = "chapters">
    <%= render @chapters %>
  </ul>
<% else %>
  pas encore de chapitre
<%end%>
