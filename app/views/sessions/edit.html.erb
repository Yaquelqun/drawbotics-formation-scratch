<h1>Modify the session <%= @session.position %></h1>

<div >
  <div >
    <%= simple_form_for(@session) do |f| %>
    <!-- <%= f.association :users, as: :check_boxes, collection: User.all, label_method: lambda {|user| user.username.camelize } %> -->
    <%= f.input :location, class: 'form-control' %>

    <%= f.input :date, class: 'form-control' %>
      <%= f.submit "Confirm changes", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
